name: Commit message validation

on:
  pull_request:
    branches: [main, dev]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: whilesmart/workflows/commits@v1
